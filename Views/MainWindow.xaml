<Window x:Class="SELF_KBM_DESIGN.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SELF_KBM_DESIGN" xmlns:local1="clr-namespace:SELF_KBM_DESIGN.Views"
        mc:Ignorable="d"
        d:DesignHeight ="1080" d:DesignWidth="1920"
        WindowState="Maximized" WindowStyle="None" ResizeMode="CanResize" WindowStartupLocation="CenterScreen"  
        Focusable="True"
        AllowsTransparency="True" Background="Transparent" StateChanged="Window_StateChanged" Loaded="Window_Loaded"  MinWidth="1280" MinHeight="720">
    <Window.Resources>
        <Style x:Key="MinimizeNewButtonStyle" BasedOn="{StaticResource WindowNewButtonStyle}" TargetType="Button">
            <Setter Property="Content">
                <Setter.Value>
                    <Image Source="pack://application:,,,/Res/iconMinimize.png" Width="15" Height="15"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MaximizeNewButtonStyle" BasedOn="{StaticResource WindowNewButtonStyle}" TargetType="Button">
            <Setter Property="Content">
                <Setter.Value>
                    <Image Source="pack://application:,,,/Res/iconMaximize.png" Width="15" Height="15"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CloseNewButtonStyle" BasedOn="{StaticResource WindowNewButtonStyle}" TargetType="Button">
            <Setter Property="Content">
                <Setter.Value>
                    <Image Source="pack://application:,,,/Res/iconClose.png" Width="20" Height="20"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Style for Login Popup -->
        <!--<Style x:Key="LoginPopupStyle" TargetType="local:Login">
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=WindowState}" Value="Maximized">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="Margin" Value="0,100,50,50"/>

                </DataTrigger>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=WindowState}" Value="Normal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="Height" Value="Auto"/>
                    <Setter Property="Margin" Value="0,0,50,50"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>-->
    </Window.Resources>

    <Grid >

        <Border Background="#F7F8FA" CornerRadius="12">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="40" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <Border Grid.Row="0" Background="#FDFDFD" Height="55" CornerRadius="12" BorderThickness="0,0,2,0" VerticalAlignment="Top" MouseDown="MouseDownHandler">
                    <Grid >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <!-- Logo -->
                        <Image Source="pack://application:,,,/Res/FD-NewLogo.ico" Width="70" Height="40" Margin="10,0,0,10" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right" Grid.Column="2" Margin="0,0,5,5" Height="35">
                            <Button x:Name="MinimizeButtonControl" Width="30" Height="30" Click="MinimizeButton_Click"  Style="{StaticResource MinimizeNewButtonStyle}" Visibility="Visible"/>
                            <Button x:Name="MaximizeButtonControl" Width="30" Height="30" Click="MaximizeButton_Click" Style="{StaticResource MaximizeNewButtonStyle}"/>
                            <Button x:Name="CloseButtonControl"    Width="30" Height="30" Click="CloseButton_Click" Style="{StaticResource CloseNewButtonStyle}"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <Grid Grid.Row="1">


                    <Grid>
                        <Grid.ColumnDefinitions>

                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="1*"/>
                            <RowDefinition Height="1*"/>
                        </Grid.RowDefinitions>
                        <Grid Background="#CEFFEC" x:Name="BannerGrid" >
                            <Grid.ColumnDefinitions>

                            </Grid.ColumnDefinitions>
                            <Grid Grid.Column="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="725*"/>
                                    <ColumnDefinition Width="15*"/>
                                    <ColumnDefinition Width="28*"/>
                                </Grid.ColumnDefinitions>

                                <Image Source="pack://application:,,,/Res/GroupBanner.PNG"  Width="Auto" Height="Auto" Margin="79,0,26,10" HorizontalAlignment="Center" VerticalAlignment="Center"/>

                            </Grid>
                            <Grid Grid.Column="1" />
                        </Grid>
                        <Grid  Grid.Column="0" Grid.Row="1" Background="#BD0017"  x:Name="SliderGrid">
                            <Grid.ColumnDefinitions>

                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="8*"/>
                                <RowDefinition Height="2*"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Column="0" Grid.Row="0" x:Name="SliderStackPanel" Margin="20,10,0,10" MaxHeight="224" MaxWidth="688">
                                <Image x:Name="SliderImage" Stretch="Uniform" />
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Grid.Column="0" Grid.Row="1" Margin="20,0,0,10" MaxHeight="25" MaxWidth="500"
                                        Orientation="Horizontal" HorizontalAlignment="Center" >

                                <ItemsControl x:Name="DotsPanel" ItemsSource="{Binding Dots}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <StackPanel Orientation="Horizontal" />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Rectangle Fill="{Binding Color}" Margin="5" MouseDown="Dot_MouseDown">
                                                    <Rectangle.Style>
                                                        <Style TargetType="Rectangle">
                                                            <Setter Property="Width" Value="10" />
                                                            <Setter Property="Height" Value="10" />
                                                            <Setter Property="RadiusX" Value="7.5" />
                                                            <Setter Property="RadiusY" Value="7.5" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding Selected}" Value="True">
                                                                    <Setter Property="Width" Value="55" />
                                                                    <Setter Property="Height" Value="10" />
                                                                    <Setter Property="RadiusX" Value="5" />
                                                                    <Setter Property="RadiusY" Value="5" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Rectangle.Style>
                                                </Rectangle>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>




                            </StackPanel>

                            <!-- Second column (empty) -->
                            <Grid Grid.Column="1" />

                        </Grid>
                    </Grid>

                    <Grid x:Name="MainGrid" >
                        <Grid.ColumnDefinitions>

                            <ColumnDefinition Width="29*"/>
                            <ColumnDefinition Width="3*"/>

                        </Grid.ColumnDefinitions>
                        <!--<local:Login x:Name="loginPopup" Visibility="Visible" HorizontalAlignment="Center" VerticalAlignment="Center" Height="500" Margin="632,27,-316,5" Width="500"  />-->
                        <!--<local:Login x:Name="loginPopup" Style="{StaticResource LoginPopupStyle}" Visibility="Visible" HorizontalAlignment="Right" VerticalAlignment="Center" Grid.ColumnSpan="2" Margin="50,-56"/>-->
                    </Grid>


                </Grid>

            </Grid>
        </Border>
        <Rectangle  x:Name="overlay" Fill="#80000000" Visibility="Collapsed" />
        <!--<local:Logout x:Name="logoutControl" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>-->

    </Grid>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" />
    </WindowChrome.WindowChrome>
</Window>