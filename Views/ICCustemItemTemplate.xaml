<Window x:Class="SELF_KBM_DESIGN.Views.ICCustemItemTemplate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SELF_KBM_DESIGN.Views"
        xmlns:local1="clr-namespace:SELF_KBM_DESIGN.Template"
        xmlns:local2="clr-namespace:SELF_KBM_DESIGN.Template"
        mc:Ignorable="d"
        Title="ICCustemItemTemplate" Height="450" Width="800">
    <Window.Resources>
        <!-- Template for Text Property -->
        <DataTemplate x:Key="TextTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelGroup" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30" />
                </Grid.RowDefinitions>
                <TextBlock Text="{Binding Label}" FontSize="14" Margin="5" Grid.Column="0" Grid.Row="0" />
                <GridSplitter Width="5" Background="DarkGray" HorizontalAlignment="Stretch" Grid.Column="1" ResizeDirection="Columns" Grid.Row="0" />
                <TextBox Text="{Binding Input}" Margin="5" Grid.Column="2" HorizontalAlignment="Stretch" Grid.Row="0" />
            </Grid>
        </DataTemplate>

        <!-- Template for Email Property -->
        <DataTemplate x:Key="EmailTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelGroup" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Label}" FontSize="14" Margin="5" Grid.Column="0" />
                <GridSplitter Width="5" Background="DarkGray" HorizontalAlignment="Stretch" Grid.Column="1" ResizeDirection="Columns" />
                <TextBox Text="{Binding Input}" Margin="5" Grid.Column="2" HorizontalAlignment="Stretch" />
            </Grid>
        </DataTemplate>

        <!-- Template for Phone Property -->
        <DataTemplate x:Key="PhoneTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelGroup" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Label}" FontSize="14" Margin="5" Grid.Column="0" />
                <GridSplitter Width="5" Background="DarkGray" HorizontalAlignment="Stretch" Grid.Column="1" ResizeDirection="Columns" />
               <!-- <TextBox Text="{Binding Input}" Margin="5" Grid.Column="2" HorizontalAlignment="Stretch" />-->
                <TextBox Text="{Binding Input, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         Margin="5" Grid.Column="2" Padding="8"
                         Background="#FFF4E6" 
                         BorderBrush="Orange" 
                         BorderThickness="2" 
                         Foreground="Black"
                         FontWeight="Bold">
                    <TextBox.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </TextBox.Resources>
                </TextBox>
            </Grid>
        </DataTemplate>

        <!-- Template for Comments (Multi-line Text) Property -->
        <DataTemplate x:Key="CommentsTemplate">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" SharedSizeGroup="LabelGroup" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding Label}" FontSize="14" Margin="5" Grid.Column="0" />
                <GridSplitter Width="5" Background="DarkGray" HorizontalAlignment="Stretch" Grid.Column="1" ResizeDirection="Columns" />
                <TextBox Text="{Binding Input}" Margin="5" Grid.Column="2" AcceptsReturn="True" Height="100" HorizontalAlignment="Stretch" />
            </Grid>
        </DataTemplate>
        <!--ListBox -->
        <!-- Template 1: Simple Text -->
        <DataTemplate x:Key="Text1Template">
            <TextBlock Text="{Binding Name}" FontSize="16" Padding="10" Foreground="DarkBlue"/>
        </DataTemplate>

        <!-- Template 2: Image + Text -->
        <DataTemplate x:Key="ImageTemplate">
            <StackPanel Orientation="Horizontal" Margin="5">
                <Image Source="{Binding ImagePath}" Width="40" Height="40" Margin="5"/>
                <TextBlock Text="{Binding Name}" FontSize="14" VerticalAlignment="Center"/>
            </StackPanel>
        </DataTemplate>

        <!-- Template 3: Card Layout -->
        <DataTemplate x:Key="CardTemplate">
            <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="8" Padding="10" Background="LightYellow" Margin="5">
                <StackPanel>
                    <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold"/>
                    <TextBlock Text="{Binding Description}" FontSize="12" Foreground="Gray"/>
                </StackPanel>
            </Border>
        </DataTemplate>

        <!-- DataTemplateSelector for Choosing Template Based on PropertyType -->
        <local1:ItemControlPropertyTemplateSelector x:Key="SettingTemplateSelector"
                                        TextTemplate="{StaticResource TextTemplate}"
                                        EmailTemplate="{StaticResource EmailTemplate}"
                                        PhoneTemplate="{StaticResource PhoneTemplate}"
                                        CommentsTemplate="{StaticResource CommentsTemplate}" />
        <local2:MyListBoxTemplateSelector  
            
            x:Key="CustomTemplateSelector"
            Text1Template="{StaticResource Text1Template}"
            ImageTemplate="{StaticResource ImageTemplate}"
            CardTemplate="{StaticResource CardTemplate}" />
    </Window.Resources>

    <!-- ItemsControl with ScrollViewer -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" BorderBrush="Brown" BorderThickness="1" Margin="10 10 10 10" CornerRadius="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <Grid IsSharedSizeScope="True" HorizontalAlignment="Stretch">
                    <ItemsControl x:Name="SettingsItemsPanel"
                          ItemsSource="{Binding ItemsTemplateSourceDataList, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                          ItemTemplateSelector="{StaticResource SettingTemplateSelector}">

                    </ItemsControl>
                </Grid>

            </ScrollViewer>
        </Border>
        <Border Grid.Column="1" BorderBrush="AliceBlue" BorderThickness="5" CornerRadius="10" Margin="10" Background="#FDFDFDFD">
            <ListBox Name="MyListBox"
            ItemTemplateSelector="{StaticResource CustomTemplateSelector}"
            Background="WhiteSmoke"
            BorderThickness="0"/>
        </Border>
    </Grid>
   
</Window>
